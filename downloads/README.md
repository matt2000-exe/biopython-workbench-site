# Downloads folder

Place the packaged, portable executable here as:

```
BiopythonWorkbench.exe
```

The download buttons on the site point to `downloads/BiopythonWorkbench.exe`. This
folder is a placeholder — swap in the real PyInstaller output before publishing.
